# cluster

tty-clock clone/remake made in rust. instead of including the ascii characters in the code it uses figlet-rs.

todo:
allow user to use different/custom figlet fonts
use q to exit without pressing enter after
non figlet mode
